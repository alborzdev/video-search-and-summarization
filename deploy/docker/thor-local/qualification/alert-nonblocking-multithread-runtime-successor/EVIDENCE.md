# Retained evidence

`runtime-receipt.json` is the retained current-Thor runtime result for official
row 327. It is hash-bound to the exact official ledger row, Alert concurrency
and metrics implementation, the active image identities, and the checked-in
small video fixture.

The receipt records the measured overlap, overtaking completion order, async
queue progress before the slowest call returned, unique dispatch thread names,
six correlated Elasticsearch outputs, and per-candidate terminal Prometheus
counts. It also proves the exact projected primary VSS state was restored and
all owned topics, consumer state, prompt keys, indices, ports, and the
disposable container were removed.
