# Current 500-row LVS MCP runtime successor

This deterministic successor combines the current 289-row canonical Thor
ledger—including exact live discovery of the complete 13-tool LVS MCP surface,
its non-inference operations, the Thor adapter's disposable file lifecycle,
adjacent-negative boundaries, and exact cleanup—with the unchanged 211
reviewed planning candidates from the prior Search Content-Type-qualified
500-row view. Candidate IDs, order, planning-only states, and evidence-empty
status remain unchanged.

The promoted capability preserves the pinned-upstream nine-tool discrepancy
and does not claim that this receipt executes the four inference/stream tools.
Those remain owned by the separate summarization, captioning, and live-stream
runtime capabilities. The Warehouse sample bundle remains excluded.

Regenerate atomically and verify:

```bash
python3 deploy/docker/thor-local/qualification/metadata-500-current-lvs-mcp-runtime-successor/project.py --write
python3 deploy/docker/thor-local/qualification/metadata-500-current-lvs-mcp-runtime-successor/project.py
python3 deploy/docker/thor-local/parity/verify_metadata_set.py --json
```

The projector performs no Docker, network, service, GPU, model, browser, or
product API operation. Its explicit write mode changes only the four
post-state documents, two descriptors, and the selected-set pointer.
