# Evidence

- Source locks bind the public 3.2.1 request/OpenAPI fields, the Thor derivative image, the current stream-handler routing fix, and the shared bounded LVS file-lifecycle implementation.
- The positive runtime oracle requires a real event with exact custom schema keys, exact enum values, numeric bounded timestamps, a fixture-grounded object, and at least one processed video chunk.
- The adjacent impossible-enum request cannot pass with malformed JSON or a nonempty event list.
- The 10-action envelope includes complete file prestate, model/readiness checks, one upload/readback, two schema requests, exact delete, complete catalog restoration, and final readiness.
- Container identity/health/restart/OOM state must remain unchanged across execution.
- Retained evidence excludes raw IDs, prompts, response content, and endpoints. Warehouse data, model staging, service lifecycle, Agent generation, and VIOS/RT-CV mutations are excluded.
