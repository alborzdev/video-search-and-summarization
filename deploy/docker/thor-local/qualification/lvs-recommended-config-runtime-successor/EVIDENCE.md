# Evidence

Runtime evidence is generated by `harness.py` and retained in
`runtime-receipt.json`. The receipt is strict-schema validated, source-locked,
privacy-safe, and bound to official row 303 by `verify.py`.

The retained run passed in 3.679263 seconds with receipt SHA-256
`393f13dd81b6e09b594d2e6117aa6ae5cb291b67a90accea8fe4e918aecfc1d8`.
It observed the exact released response fields `chunk_size` and `text`, a
recommended chunk size of 60, HTTP 422 for each zero-valued bounded input, and
one successful local Cosmos follow-on using chunk duration 60 unchanged. The
owned fixture was deleted and all protected baselines matched before/after.
