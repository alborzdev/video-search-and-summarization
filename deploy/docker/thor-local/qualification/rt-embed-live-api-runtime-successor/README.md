# RT-Embed live stream and complete API runtime successor

This package binds official capabilities 369, 373, and 374 to fresh current-Thor
runtime evidence. It source-locks and reuses the repository's comprehensive
RT-Embed qualifier while applying exactly two transparent current overlays: the
current Thor Compose digest and capture date.

The default command is inert. Acknowledged execution verifies the existing model
and Triton trees, launches three disposable local helper containers, publishes
the tracked H.264 fixture to an RTSP listener reachable only from RT-Embed's
private Docker gateway, generates a real live embedding, and exercises every one
of the 24 documented `/v1` operations. Exact cleanup is required.

```bash
PYTHONDONTWRITEBYTECODE=1 python3 execute.py plan
PYTHONDONTWRITEBYTECODE=1 python3 execute.py execute \
  --ack I_ACK_CURRENT_RT_EMBED_LIVE_API_AND_EXACT_REVERSIBLE_CLEANUP
```
