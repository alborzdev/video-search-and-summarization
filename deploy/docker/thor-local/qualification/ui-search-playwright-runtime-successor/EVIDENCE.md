# Evidence

Runtime evidence is generated by `harness.mjs` and retained in
`runtime-receipt.json`. The verifier binds the receipt to the exact Search
backend/UI sources and refuses to treat this package as promotion-eligible.

The positive boundary includes a real indexed-video result, a playable modal,
a real VST frame, responsive desktop/mobile rendering, and a fresh sidebar
agent result with a locally generated `Confirmed` critic verdict. The adjacent
boundary is equally important: the same real frame contains no stored bbox
metadata, so selected-object image KNN remains open rather than being simulated
or inferred from unit tests.

On 2026-08-10, target commit
`cea6897406e09afe341c4a5a9817364b77ff6c03` completed the retained run in
37.525 seconds and 18 browser actions. The direct Search call returned HTTP 200
and rendered one real result at 0.40 similarity. Playback opened and the image
flow rendered a real frame, then explicitly reported that the frame contained
no bounding boxes. Desktop 1440x900 and mobile 390x844 both had zero horizontal
overflow.

A second isolated browser context sent a fresh archive-search request through
the sidebar VSS Agent. It rendered one result card with a `Confirmed` local
critic verdict; all four displayed criteria were true and none were false.
The input re-enabled after completion. Across both contexts the browser observed
100 numeric-loopback HTTP responses, all 200 or 206, with no failed responses,
page errors, console warnings/errors, or non-loopback HTTP traffic.

The retained receipt contains only hashes, counts, labels, dimensions, status
codes, and the explicit open-corpus boundary. Its SHA-256 is established by the
repository gate after this file set is finalized.
