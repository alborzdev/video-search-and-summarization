# Evidence boundary

The producer verifies 42 repository source locks, six exact canonical oracle
rows, six exact official-capability rows, and four image identities recorded in
existing repository evidence. Four canonical rows remain open. Byte-identical
full-file download and CPU multimedia are independently `passed_current` with
exact runtime-evidence bindings. It verifies the source-level upload-limit fix
and compiles fixture argv as inert data. The fix is scoped to each request's
declared `Content-Length`; multipart overhead and aggregate multi-request file
size remain explicit boundaries. This producer observes zero runtime actions
and is not itself runtime evidence for any capability or a replacement for the
separate lifecycle and codec receipts.
