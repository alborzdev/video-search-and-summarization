# Evidence boundary

The producer verifies 42 repository source locks, six exact canonical oracle
rows, six exact official-capability rows, and four image identities recorded in
existing repository evidence. It verifies the source-level upload-limit fix and
compiles fixture argv as inert data. The fix is scoped to each request's
declared `Content-Length`; multipart overhead and aggregate multi-request file
size remain explicit boundaries. It observes zero runtime actions and is not
evidence that any of the six capabilities works on Thor.
