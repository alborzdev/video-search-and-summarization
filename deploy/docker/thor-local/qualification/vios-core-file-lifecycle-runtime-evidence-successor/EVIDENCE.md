# Evidence boundary

The producer verifies 42 repository source locks, six exact canonical oracle
rows, six exact official-capability rows, and four image identities recorded in
existing repository evidence. It detects one source-level product gap and
compiles fixture argv as inert data. It observes zero runtime actions and is
not evidence that any of the six capabilities works on Thor.
