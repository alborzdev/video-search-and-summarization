# Evidence boundary

## Mechanically established

The compiler verifies the exact ten advertised candidate identities against
both the current candidate-approval mapping and advertised-entry inventory. It
requires every authoritative mapping row to retain `binding_kind: none`,
`no_receipt_not_admitted_not_executable`, no required cloud inference, and no
Warehouse dependency. It validates the exact candidate classifications and
executor roles, all direct file digests, all nested package source locks, and
the checked-in deterministic overlay schema.

The resulting counts are fixed at 10 concrete implementations, 0 partial
implementations, and zero executor-ready, canonical/full bindings, canonical
changes, admissions, executable rows, receipts, or promotions.

## Not established

No executor was invoked. No HTTP, browser, Docker, service, model, credential,
download, media mutation, or runtime action occurred. “Concrete” describes
implemented candidate behavior only; it is not live evidence. The Base, LVS,
Search, and UI retained gaps in `contract.json` remain open. This package
cannot advance canonical state and contains no runtime-evidence or admission
input.
