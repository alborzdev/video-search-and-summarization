# Evidence boundary

The inventory locks all three canonical planning rows/payloads/capabilities/oracles and the exact 16-file alert product/test denominator used to justify these static checks. The executor independently re-hashes every locked file before importing product code.

Positive observations:

- DirectMedia verification yields a confirmed Cosmos verdict; context preserves raw reasoning; classification applies the pluggable parser. All three return explicit acknowledged fake-sink receipts.
- `nv.Incident` and `nv.Behavior` survive deterministic JSON→Protobuf→JSON→Protobuf checks. Incident field 7 accepts both documented `analytics` and released `analyticsModule` spellings.
- Actual Elasticsearch sink code distinguishes acknowledged, unconfirmed, and failed fake-client results. Actual Kafka sink code distinguishes callback acknowledgement, callback failure, undelivered failure, and drained-but-unconfirmed submission.
- Actual `TerminalJobStore` plus the DirectMedia before-publish hook suppresses every sink call after accepted cancellation.

Adjacent negatives cover unsupported mode selection, non-object classification output, conflicting Incident aliases, invalid protobuf bytes, malformed Elasticsearch acknowledgement, Kafka callback error, missing Kafka callback, and cancellation before publish.

These observations cannot mark a capability `passed_current`, cannot advance a planning row or oracle, and contain no runtime evidence. The Warehouse sample bundle is explicitly excluded.
