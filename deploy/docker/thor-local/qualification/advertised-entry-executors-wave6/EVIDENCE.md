# Wave-six evidence boundary

## Positive static observations

- Exact denominator: 87 total advertised gaps, 63 predecessor candidates, 24
  prior open entries, 8 selected here, and 16 left open.
- Seven exact `vios-ui` manifest literals and the exact `NAT generate/chat`
  literal resolve through the digest-locked plan and manifest.
- The dashboard, sensor/stream management, record-settings, and
  media-management component routes and related source compositions are
  present.
- Live, replay, video-wall, QoS/system/stream-stats, debug, and playback
  automation component sources are present.
- The current route table maps live, recorded, video-wall, and Experimental to
  empty fragments and does not import their page components. This is recorded
  as a static mismatch, not hidden.
- The agent pins `nvidia-nat==1.6.0`, its custom worker inherits NAT's route
  registration, and the checked-in static agent API inventory contains POST
  generate/chat variants.

## Explicit non-claims

This package does not claim any browser interaction, DOM render, route
reachability, UI build, API execution, backend-state correlation, media upload,
recording mutation, live/replay playback, video-wall output, polling result,
WebSocket session, NAT import, request/response transcript, MCP execution,
service readiness, runtime evidence, or official capability advancement.

It performs no network access, Docker or subprocess invocation, service
lifecycle action, credential access, download, or filesystem write. It does not
use the optional Warehouse sample. All eight required runtime oracles remain
`open_unexecuted`, and every emitted official capability effect is
`none_candidate_only`.
