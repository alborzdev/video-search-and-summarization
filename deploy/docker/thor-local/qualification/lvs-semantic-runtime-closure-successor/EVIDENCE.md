# Evidence statement

## Construction evidence

- `executor.py plan` validates every source lock, the current 500-row selected
  metadata set, the unchanged open/evidence-empty/null-executor LVS row, its
  frozen 14-request bound, both JSON schemas, the additive 24-request workflow,
  and every nonpromotion flag.
- The focused mock transport suite covers authorization-before-I/O, exact five
  tool discovery, exact VST timeline and stored-media byte readback, distinct
  fixture identity, build/model readiness, exact Elasticsearch collection
  preabsence, live generation acceptance, delayed Logstash delivery with
  bounded polling, exact-stream/model nonempty CA-RAG retrieval, complete VST timeline preservation, exact
  RT-VLM stop, stable post-stop delivery quiescence, owned-collection deletion,
  delayed absence, and receipt validation.
- Negative tests reject preexisting caption documents, stored-byte drift,
  unrelated timeline drift, duplicate fixture identities, response-derived
  media URLs outside the explicit numeric-loopback allowlist, delayed document
  reappearance, and cleanup failures. Separate tests prove cleanup still runs
  after ambiguous start and post-start retrieval failures.
- Agent production tests cover all-five resolution and fail-closed missing/None
  resolution for the new read-only discovery helper.

## Runtime evidence

No runtime evidence exists yet. No live HTTP request, caption generation,
service lifecycle operation, Docker operation, download, credential access, or
Warehouse sample operation was performed during construction.

A future live receipt is valid only when it conforms to `receipt.schema.json`.
It contains hashes, counts, durations, statuses, bounded observations, and
coverage facts; it excludes raw tool payloads, raw media, prompts, URLs,
resource IDs, and credentials. Even a valid receipt is explicitly
nonpromoting until the six residual semantic gaps are closed and the
canonical bound is reconciled.
