# Evidence

`matrix.json` binds twenty exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 8 adds asset limits and expiry at official index 364 using the mounted production `AssetManager`: oldest-idle pressure eviction, protected in-use assets, exact `ServerBusy`/503 hard-cap rejection, and TTL expiry all passed. All isolated assets were cleaned and the main RT-VLM catalog stayed pristine. All twenty rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
