# Evidence

`matrix.json` binds seven exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 2 adds file upload at official index 336 and dense captions at index 342. All seven rows have current local runtime evidence, exact cleanup assertions, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
