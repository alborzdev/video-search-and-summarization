# Evidence

`matrix.json` binds thirteen exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 3 adds the six direct RT-VLM OpenAI-compatible API rows at official indices 347, 348, 349, 350, 353, and 354. All thirteen rows have current local runtime evidence, exact cleanup assertions, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
