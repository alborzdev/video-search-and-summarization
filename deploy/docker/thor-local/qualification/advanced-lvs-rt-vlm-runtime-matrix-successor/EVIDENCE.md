# Evidence

`matrix.json` binds forty-two exact VSS 3.2.1 advertised rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 20 adds VIOS indices 412, 415, and 416. A namespaced 15-request run uploaded and registered a deterministic H.264/AAC file, rejected the duplicate name, downloaded the full file byte-identically, generated a distinct 1.2-second H.264 MP4 clip from runtime-derived timeline bounds, and returned a decodable timestamped 160x120 MJPEG snapshot correlated to the owned source pixels. Exact pre/post sensor and file inventories matched. All forty-two rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
