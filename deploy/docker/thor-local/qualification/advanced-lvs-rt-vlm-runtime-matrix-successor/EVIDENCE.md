# Evidence

`matrix.json` binds fifteen exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 4 adds allowlisted file URI and inline data URI at official indices 339 and 340. All fifteen rows have current local runtime evidence, exact cleanup assertions, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
