# Evidence

`matrix.json` binds thirty-nine exact VSS 3.2.1 advertised rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 19 adds behavior indices 69 and 70: a real RT-CV 2D RADIO-CLIP run used smart vision-encoder inference and optical-flow-assisted prediction while the exact signed v1.0 NGC artifact emitted finite 1,536-dimensional vectors for Pallet and Person detections across 15 object/track pairs. The deterministic two-cycle RTSP proof re-associated one object across different tracker IDs with minimum cosine 0.9997989146647734, exercised lifecycle and metrics APIs, rejected an incompatible 1,024-dimension admission, and cleaned up exactly. All thirty-nine rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
