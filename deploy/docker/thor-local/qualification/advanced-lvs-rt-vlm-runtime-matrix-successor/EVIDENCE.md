# Evidence

`matrix.json` binds forty-eight exact VSS 3.2.1 advertised rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 23 adds VIOS index 413. A bounded isolated NvStreamer run accepted API upload, shipped-UI upload, and local-mount input; automatically produced H.264/AAC RTSP; decoded a live snapshot and an advancing WebRTC frame; rejected invalid media with HTTP 400; and restored exact Docker and owned-resource state. Overlay 22's codec evidence remains exact, including the direct H.264+B-frame+AAC limitation and working transcode substitute; row 423 audio recording is not claimed. All forty-eight rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
