# Evidence

`matrix.json` binds twenty-nine exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 13 adds same-byte URL/inline-base64 video at index 370 and Kafka/Redis/OpenTelemetry at 372: two finite embedding paths, two consumed Kafka protobufs, one Redis error, and correlated broker/request spans passed with exact cleanup. All twenty-nine rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
