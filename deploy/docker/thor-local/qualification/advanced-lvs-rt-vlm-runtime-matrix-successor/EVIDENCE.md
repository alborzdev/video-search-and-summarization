# Evidence

`matrix.json` binds thirty-seven exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 18 adds RT-CV 2D index 377: the exact signed RADIO-CLIP v1.0 artifact emitted finite 1,536-dimensional vectors for Pallet and Person detections across 17 object/track pairs. A deterministic two-cycle RTSP proof re-associated one object across different tracker IDs with minimum cosine 0.9999958799323881, exercised lifecycle and metrics APIs, rejected an incompatible 1,024-dimension admission, and cleaned up exactly. All thirty-seven rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
