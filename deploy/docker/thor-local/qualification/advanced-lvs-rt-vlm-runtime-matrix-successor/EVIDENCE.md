# Evidence

`matrix.json` binds forty-seven exact VSS 3.2.1 advertised rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 22 adds VIOS indices 420, 421, 422, and 424. A bounded isolated Thor run decoded 120 H.264 frames from a two-B-frame source, decoded 120 HEVC frames from four-slice pictures through RTSP, exercised hardware and CPU/software H.264/H.265 media paths, and observed 48 kHz AAC on supported H.264/HEVC RTSP output. The direct H.264+B-frame+AAC limitation and working software-transcode substitute are both retained; row 423 audio recording is not claimed. All forty-seven rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
