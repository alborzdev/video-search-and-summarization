# Evidence

`matrix.json` binds thirty-four exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 15 adds RT-CV 2D index 379: the real on-demand REST endpoint returned repeatable finite 1,152-dimensional SigLIP2 vectors for one P6 PPM image, a distinct vector for a second image at cosine 0.8704016749813162, and an exact fail-closed error for a missing image, all with zero active streams and exact cleanup. All thirty-four rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate Thor JPEG decoder defect is explicitly outside this passing format-specific claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
