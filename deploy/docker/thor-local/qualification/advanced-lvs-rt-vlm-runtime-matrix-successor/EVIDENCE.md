# Evidence

`matrix.json` binds thirty-three exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 14 adds RT-CV 2D indices 375, 380, 381, and 382: real file and RTSP detection/tracking with 1,152-dimensional embeddings, exact dynamic lifecycle, health/Prometheus metrics, and stable Thor VPI tracker tuning all passed with exact cleanup. All thirty-three rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
