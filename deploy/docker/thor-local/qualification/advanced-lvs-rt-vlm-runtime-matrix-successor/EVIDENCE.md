# Evidence

`matrix.json` binds five schema-valid live Thor receipts to exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger. All five have current local runtime evidence, exact cleanup assertions, zero VSS Agent `/generate` calls, and no Warehouse sample dependency.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
