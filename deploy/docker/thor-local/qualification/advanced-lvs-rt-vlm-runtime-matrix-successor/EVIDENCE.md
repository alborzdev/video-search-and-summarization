# Evidence

`matrix.json` binds forty-three exact VSS 3.2.1 advertised rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 21 adds VIOS index 417: fresh Chromium/Playwright live and replay WebRTC sessions completed signaling, exposed live unmuted video tracks, decoded 25 and 53 frames, and kept replay advancing through API seek, UI seek, and an expected HTTP 501 unsupported seek. Distinct route families, timing semantics, session lifecycles, and exact teardown were observed. Overlay 20's indices 412, 415, and 416 retain their exact file-lifecycle evidence. All forty-three rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
