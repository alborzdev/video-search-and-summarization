# Evidence

`matrix.json` binds thirty-six exact VSS 3.2.1 advertised-entry rows in the selected Metadata500 ledger to schema-valid live Thor receipts. Overlay 17 adds RT-CV 2D index 378: the exact SigLIP2 v1.1 runtime emitted finite 1,152-dimensional appearance vectors across a deterministic two-cycle RTSP occlusion/re-entry fixture and re-associated a known object across different tracker IDs with minimum cosine 0.9999663695948764. The proof also exercised real lifecycle and metrics APIs, rejected a mismatched tokenizer before launch, and cleaned up exactly. All thirty-six rows have current local runtime evidence, zero VSS Agent `/generate` calls, and no Warehouse sample dependency. The separate row-379 Thor JPEG decoder defect remains explicitly outside that passing P6-PPM-scoped claim.

This is a runtime-evidence overlay, not a trusted canonical admission receipt. Canonical candidate state remains explicitly unchanged and visible in every matrix row.
