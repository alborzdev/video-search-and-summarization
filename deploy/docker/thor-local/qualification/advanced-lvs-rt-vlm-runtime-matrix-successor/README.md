# Advanced LVS and RT-VLM runtime matrix successor

Overlay 50 binds ninety-one advertised candidate rows to current-Thor live
receipts. Its new row proves LVS Prometheus metrics around one bounded local
Cosmos caption request: the completed-query gauge and six VLM histogram counts
advanced exactly once, pending returned to zero, latest latency gauges were
positive, and the metrics carried no resource-ID label cardinality. File,
Neo4j, LVS runtime, and running-container baselines were restored exactly.

Overlay 49's two rows prove both RT-VLM stream-management API families
against one genuine local RTSP publisher. The original plural API preserved a
fixed external identity and place metadata and returned mixed batch-delete
outcomes; the CV-compatible singular API preserved camera identity, accepted
CV metadata/headers, rejected a duplicate, and removed by camera ID. The
generated ledger reverses those endpoint families, so the package records that
metadata defect and follows the released source and live OpenAPI. Exact stream
catalog and container baselines were restored with zero inference calls.

Overlay 48's row proves non-blocking multithreaded Alert execution: six
protobuf candidates entered a one-worker ingestion path, all queued before a
deliberately slow VLM response completed, three VLM calls overlapped, and all
five independent fast candidates overtook the slow one. Every candidate kept
its correlated media, category, JSON-parser verdict/reasoning, Elasticsearch
output, and terminal per-sensor metrics with zero inline fallbacks and exact
cleanup.

Overlay 47 adds configured custom VLM parsing: an operator-owned external class loaded in both Alert processes, normalized a real local Cosmos response into a parser-identified structured verdict/reasoning payload, persisted the exact pluggable response schema, and rejected an invalid import before health while restoring exact state. Overlay 46 adds multi-category classification: two temporary mappings—including a case-normalized alias—produced distinct persisted categories with deterministic confirmed verdicts, reasoning, and `OK` parse status on the local Cosmos 3 VLM. Overlay 45 adds contextualization: scene, time, object, and media context stayed traceable to the upstream candidate and remained structurally separate from category, verdict, and status fields. Overlay 44 adds post-alert verification: after Behavior Analytics created an upstream candidate, Alert Bridge re-evaluated its associated interval and preserved document/source identity, sensor, objects, and timestamps while adding a confirmed verdict, reasoning, and `OK` status. Overlay 43 adds the complete CV perception → Behavior Analytics → local VLM verification chain: one temporary VIOS RTSP camera produced tracked RT-CV person metadata, the real Behavior Analytics state machine emitted a candidate, and Alert Bridge persisted a confirmed local Cosmos 3 verdict under the same candidate ID, sensor identity, evidence interval, object IDs, and object timeline. Four VIOS snapshots were transported as base64 data URLs, and all owned documents, recording, temporary media, camera/proxy state, and publisher state were removed after a 15-second asynchronous drain. Overlay 42 adds operator-triggered on-demand alert verification: a locally served clip reached a server-generated job's deterministic `completed / verified / confirmed` terminal verdict through the local Cosmos 3 VLM, its acknowledged Elasticsearch result was read back, independent pre-publish cancellation and unknown-category rejection passed, and exact state was restored. Overlay 41 adds natural-language live-stream VLM rules: nine bounded windows covered a complete deterministic blue/green/red RTSP cycle, exactly the three contiguous green windows emitted correlated incidents, and all six blue/red windows remained non-alerts. Overlay 40 adds immutable realtime rule CRUD: invalid and unknown mutations were inert, replacement used a distinct rule and RT-VLM request identity on one shared stream, old-rule deletion stopped only the old request, the replacement emitted a correctly correlated incident, and exact unrelated state was restored. Overlay 39 adds rule-scoped incident retrieval: one real RT-VLM alert retained stable rule, stream, and sensor identity; a six-filter query excluded four single-bound controls; unknown and malformed rule identities followed the declared negative behavior; and exact runtime state was restored. Overlay 38 added the always-on lifecycle: one declared local rule started, duplicate events deduped, Alert Bridge restart recovery replaced the surviving RT-VLM worker exactly once, and camera removal restored exact state. Overlay 37 added idempotent realtime rule replay: one persisted local rule was replayed twice with stable rule/config/stream identity, exactly one observable activation after each replay, and exact cleanup. Overlay 36 added the rendered Alerts View/Manage and real-time rule-management row. A fresh Chromium session created a uniquely owned RTSP sensor through Video Management, selected it from the VIOS catalog, persisted and rendered one active rule, deleted the rule and sensor through the UI, and restored all unrelated active state exactly. The deployed fix reconciles VIOS's differing live-proxy and canonical sensor-stream URLs by exact `streamId`, so Alert Bridge safely reuses the RT-VLM registration. Overlay 35's four RT-VLM URL-ingestion security rows and all earlier evidence remain unchanged. External Slack and audio recording remain explicitly unclaimed.

Each receipt is hash-locked, validated against its own JSON Schema, bound to its exact package contract, checked for required cleanup and policy assertions, and mapped to the exact row/index in the selected 500-row metadata set. The resulting `matrix.json` truthfully records `passed_current_thor` runtime evidence.

The canonical candidate-admission system still records these rows as `not_qualified` because it has no trusted admission receipts. This overlay does not rewrite that separate state or claim canonical promotion. It provides evidence-based local feature accounting while keeping the protected admission drafts untouched. VSS Agent `/generate` calls and the Warehouse sample are excluded. The custom sink is an extension implementation, not a new built-in NVIDIA `sinkType`; 3D/MV3DT remains outside this Behavior Analytics proof. Row 377 binds 1,536 dimensions to the exact signed RADIO-CLIP v1.0 artifact despite conflicting NVIDIA documentation. The row-378 claim is appearance-based re-association rather than legacy tracker-ID continuity. The row-379 claim remains P6-PPM-scoped; the separately observed Thor JPEG decoder defect remains open and is not represented as passing.

```bash
PYTHONDONTWRITEBYTECODE=1 python3 \
  deploy/docker/thor-local/qualification/advanced-lvs-rt-vlm-runtime-matrix-successor/compiler.py check
PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q \
  deploy/docker/thor-local/qualification/advanced-lvs-rt-vlm-runtime-matrix-successor/tests/test_compiler.py
```
