# Evidence boundary

## Established statically

- The exact open advertised row remains `required_local`, `thor_state=wired`,
  `runtime_state=not_qualified`, and Warehouse-excluded.
- Two ordered, digest-bound video recipes carry reciprocal disjoint event
  tokens and durations above the current 60-second LVS routing threshold.
- Four static artifact observations cover the exact two-source by
  Markdown/PDF cross-product.
- The actual checked-in `_correlate_video_reports` and
  `_video_report_observability` helpers preserve ordered source identity,
  source index/count, object keys, and one shared request correlation ID.
- Adversarial tests reject missing sources, duplicate extensions, foreign or
  swapped attribution, cross-source event contamination, stem mismatch, and
  digest drift.

## Not established

- No video bytes were rendered; `materialized_video_sha256` is null for both
  recipes.
- No VST, LVS, RT-VLM, Agent, object-store, PDF parser, or model endpoint was
  called.
- No live report bytes or extracted PDF text were semantically evaluated.
- No runtime identity, pre-state, cleanup, quiescence, receipt, admission, or
  canonical-state change exists.

Therefore the correct classification is
`concrete_static_candidate`, not a runtime pass.
