# Evidence boundary

The successor proves source identity and static command construction only. It
does not replay the multi-gigabyte verifier, inspect a Docker volume, authorize
a runtime action, or create runtime evidence. The predecessor's 16 approval
bundles and zero approvals/receipts/executions remain unchanged.
