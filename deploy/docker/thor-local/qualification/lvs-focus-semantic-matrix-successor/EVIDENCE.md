# Evidence

- The current LVS HTTP and Agent-session predecessors, GA OpenAPI, server/model request handling, and LVS Agent config are source-locked.
- The six-case matrix covers object, event, and scenario independently; their combined target relationship; a planted distractor control; and an absent-focus negative over one digest-pinned uploaded source.
- Request bodies and correlated response artifacts are hashed independently. Response source ID/model, caption-source digest, unique response UUID, standard content shape, timestamped events, requested markers, and absent-event exclusion are checked before a case passes.
- The 14/14 action envelope is exact and includes cleanup/restoration; no predecessor-plus-supplement overrun remains in this candidate.
- Unit tests are fake-only. The retained runtime receipt records the separately authorized live Thor execution; no service lifecycle, model download, or Warehouse action occurred.
- The live receipt proves six current local model calls and exact file cleanup. The public response correlates every case to the same uploaded file ID and digest but does not expose an internal caption-set digest proving that all six backend calls reused byte-identical generated captions. Full unrelated Agent report/prompt state remains outside the LVS file API.
