# Evidence

`runtime-receipt.json` is generated by the bounded live harness and validated
against `receipt.schema.json`. It retains only content hashes and deterministic
semantic booleans, never raw prompts, captions, summaries, resource IDs, or
credentials.

The retained Thor run passed both local inference routes against the 10-second
fixture. `/generate_vlm_captions` returned four exact intervals
(`0-3`, `3-6`, `6-9`, and `9-10`) and identified the beginning box-carrying
action plus the ending ladder/shelf action. `/v1/summarize` then returned two
chronological events covering `0-9` and `9-10`, preserved both endpoint events,
excluded the absent forklift control, and recorded positive caption and
aggregation token usage.

The run used image
`sha256:598dc5123f949dd0f495d1e93d85450df6045fee30dc9c98a4eab1226fe39b7d`,
which contains the full-file timestamp guard and exact interval grounding for
plain-caption structured inference. `vss-lvs` remained healthy with zero
restarts and no OOM. The owned upload was deleted, the file catalog and Neo4j
counts returned exactly to their starting values, the running container set was
unchanged, and free space remained above the 10 GiB safety floor.

Receipt SHA-256:
`9aa5d062b2574b7b65da3c67e9bac03c501acfc276f1a0af037318d84a644f7f`.
