# Evidence boundary

The retained runtime receipt is intended to prove exact 13-tool discovery,
schema presence, live SSE initialization, five non-inference calls, a complete
owned file lifecycle, two adjacent negative controls, and exact cleanup on the
current Thor runtime.

It does not claim semantic execution of the four inference/stream tools; those
remain covered by their separate summarization, captioning, and live-stream
capabilities. No Warehouse data, RTSP stream, Agent `/generate` request, service
lifecycle action, raw identifier, raw response, or credential is retained.

The 2026-08-11 Thor run passed all 13 calls in 1.484 seconds: 11 expected
successes and two sanitized adjacent-negative responses. The exact MCP, REST,
and media-root catalogs were restored; the LVS container identity, healthy
state, start time, and restart count were unchanged; and no fallback deletion
was needed. `runtime-receipt.json` is the canonical machine receipt and
`official-runtime-evidence.json` is its redacted reviewer summary.
