# Cosmos3 and Smart City headline static executor

This package binds three already-canonical VSS 3.2.1 headline records to their
canonical oracles and runs the strongest provider-free, process-free checks
available from checked-in files:

- the RT-VLM/agent default is the exact Cosmos3 Nano BF16 artifact;
- the Thor Edge served VLM ID and `cosmos-reason3` selector are wired through
  the official-edge contract, environment, Compose, and agent overlay; and
- the three conflicting Smart City version statements and the exact existing
  Wave 6 `smartcity-version-lock` planning row remain explicit.

Run it with:

```bash
python3 deploy/docker/thor-local/qualification/headline-cosmos-smartcity-static-executor/executor.py
```

The executor does not use the network, Docker, a service, a model, host
prerequisite inspection, or the Warehouse sample. A passing receipt is static
identity evidence only. All three canonical runtime states remain
`not_qualified` and their oracles remain open.

See [EVIDENCE.md](EVIDENCE.md) for the exact claim boundary.
