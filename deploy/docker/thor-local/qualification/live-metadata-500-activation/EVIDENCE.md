# Metadata-500 activation evidence

Status: **the atomic metadata-default activation is applied, and its projection
is reproducible in both the pre-activation and applied states**.

Immutable pre-activation package snapshots:

- selector raw SHA-256:
  `aafab3b6e665f4f305738ed498ad2fd6dc3399d41f1d4b4b9c4ee670db5bcb15`;
- staged descriptor raw SHA-256:
  `dad58ec5c128b38235a3fc135cd1c66a0225fa3a80fa2f1f9b9cb42bce8b385c`.

The package proves the two applied metadata-control changes:

- the registered Metadata-500 descriptor changes only `/lifecycle`, from
  `validation_only` to `live_ready`;
- the selector changes only `/selected_set` and the registered Metadata-500
  descriptor raw hash. Its canonical descriptor path remains unchanged.

Frozen projected identities:

- live-ready descriptor raw SHA-256:
  `56ed8f555c0814e80a39c15198a33c80d400c991a4857a27b4609ea88b5750f9`;
- selector raw SHA-256:
  `8021efc4684b106539a627b3ddfd82937c7ecf1d6111a3c87538fc5ea7360bec`;
- activation receipt raw SHA-256:
  `79de3432908d79ca5861b77c40304f26014f1a704c6bacb1ff5a45ae00208d43`;
- exact receipt schema raw SHA-256:
  `396f8b1a5776c20b37cb372ba8dffebf0e97bdacffc28fbcd28d4e2b20ed1bae`;
- activation canonical payload SHA-256:
  `eabbd6869e139ce3b671b14489e7f2011fbb3dd723667e134e584fec9d51e657`.

The isolated default resolves and authoritatively validates at 500
capabilities, 500 ordered oracles, 55 features, 126 sources, and 47
discrepancies. The v2 validator independently reports 289 preserved v1 rows,
211 planning candidates, 23 protocol bindings, 60 workload bindings, zero
candidate evidence, and zero executor-ready candidates.

The receipt raw-locks the immutable pre-selector and pre-descriptor snapshots,
the 289 descriptor, migration proof and five post-state targets, resolver and
schemas, v1/v2 and official validators, atomic bundle verifier, and their
relevant tests. Its transition journal retains the canonical live paths and
the exact pre/applied hash pairs. The compiler accepts either complete pair and
rejects mixed, partial, or unknown control states. Its generated bytes do not
contain the observed state.

The compiler performs no runtime action, canonical write, service, Docker,
host, network, model, download, or Warehouse sample action. The reviewed
transaction installed only the exact descriptor/selector pair and wrapper/doc
expectations. Rollback restores the snapshotted 289 selector and
`validation_only` descriptor hashes; partial activation is forbidden.
